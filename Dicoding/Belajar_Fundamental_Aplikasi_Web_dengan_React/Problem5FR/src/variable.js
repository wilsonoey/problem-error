const variable = {
  HOSTDB: 'localhost',
  USERDB: 'root',
  PASSDB: '',
  NAMEDB: 'wilsonoey_kamiada',
  ACCESSJWT: 'GET_FROM_node->require('crypto').randomBytes(64).toString('hex');_IN_TERMINAL',
  REFRESHJWT: 'GET_FROM_node->require('crypto').randomBytes(64).toString('hex');_IN_TERMINAL',
  ACCESS_TOKEN_AGE: 86400,
  NODE_ENV: 'development',
}

module.exports = variable;
